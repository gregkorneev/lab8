# Лабораторная работа 8
## Алгоритм A*. Поиск безопасного пути через лес во время пожара

### Вариант: *Поиск безопасного пути через лес во время пожара — обход очагов возгорания и непроходимых зарослей*

---

## ▶️ Запуск проекта

Полная команда сборки, запуска алгоритма и построения визуализации в одну строку:

```bash
rm -rf build && cmake -B build && cmake --build build && ./build/lab8 && python3 .py/plot_forest_path.py
```
---

## 📌 Описание проекта

Проект реализует три алгоритма поиска пути:

- **A\*** — классический алгоритм поиска кратчайшего пути
- **A\* + пост-сглаживание (A\*PS)** — улучшенная версия с устранением лишних точек
- **Theta\*** — any-angle поиск с прямой видимостью между вершинами

Поиск выполняется на случайно сгенерированной карте леса:

- `.` — свободный участок леса
- `#` — огонь / непроходимые заросли
- `S` — стартовая точка (человек)
- `G` — безопасная зона

После выполнения алгоритма:
- найденный путь сохраняется в **CSV**
- строится визуализация пути в **PNG**
- данные помещаются в `data/csv/` и `data/png/`

---

## ⚙️ Возможности

✔ Случайная генерация карты леса
✔ Реализация A\*, A\*PS, Theta\*
✔ Вывод найденного пути в консоль
✔ Сохранение таблицы результата в `data/csv/forest_path.csv`
✔ Генерация PNG-визуализации пути
✔ Автоматическое создание папок `data/csv` и `data/png`
✔ Поддержка карт любых размеров

---

📊 Пример результата

После выполнения в проекте появятся:

data/csv/forest_path.csv
data/png/forest_path.png


CSV содержит строки:

row,col,cell,is_path
0,0,S,1
0,1,.,1
0,2,#,0
...


PNG — визуальная карта с легендой и аннотацией (размер карты, количество препятствий, длина пути).

📈 Метрики (по ТЗ лабораторной работы)

Проект формирует данные для анализа:

длина найденного пути

количество препятствий

количество раскрытых узлов

геометрическая длина маршрута

визуальная структура пути
